Objetos, banco de dados relacional, de  rotina (job) que valida os acessos a registro de clientes, utilizando SQL dinâmico para validar os caso no cadastro de regras e exceções.


Objects, relational databases, routine (job) that validate access to customer records, using dynamic SQL to validate the case in the registration of rules and approaches.